// Firefox Enhancements v0.1.44 - privileged AutoConfig script
// Artllex cooperative AutoConfig v1
(function () {
  if (globalThis.__artllexStarted_fe) return;
  globalThis.__artllexStarted_fe = true;
try {
  (function () {
    var MAIN_HISTORY_BUTTON_ID = "appMenu-history-button";
    var RESTORE_SESSION_BUTTON_ID = "appMenu-restoreSession";
    var CLEAR_HISTORY_BUTTON_ID = "appMenuClearRecentHistory";
    var SECRET_HELPER_NAME = "firefox_secret_window.ps1";
    var SECRET_LAUNCHER_NAME = "firefox_secret_window.vbs";
    var secretHelperStarted = false;
    var secretMediaGuardStarted = false;
    var secretMediaGuardHidden = false;
    var secretMediaMuteState = new Map();

    function saveContextImage(win) {
      var popup = win.document.getElementById("contentAreaContextMenu");
      var item = win.document.getElementById("context-saveimage");
      var context = win.gContextMenu;
      if (!popup || popup.state !== "open" || !context ||
          !(context.onImage || context.onCanvas) || !item || item.hidden ||
          item.disabled || item.getAttribute("disabled") === "true") return false;
      // Start the native action before hiding: popuphidden clears gContextMenu.
      context.saveImage();
      popup.hidePopup();
      return true;
    }

    async function saveHoveredImage(win) {
      var browser = win.gBrowser && win.gBrowser.selectedBrowser;
      if (!browser || !win.document.hasFocus()) return;
      var contexts = [];
      function collect(context) {
        contexts.push(context);
        for (var child of context.children) collect(child);
      }
      collect(browser.browsingContext);
      for (var context of contexts) {
        var manager = context.currentWindowGlobal;
        if (!manager) continue;
        var image;
        try { image = await manager.getActor("FirefoxEnhancementsHover").sendQuery("GetHoveredImage"); }
        catch (error) { reportError(error); continue; }
        if (!image) continue;
        if (win.gBrowser.selectedBrowser !== browser || context.currentWindowGlobal !== manager ||
            !win.document.hasFocus()) return;
        if (!Services.policies.isAllowed("filepickers")) return;
        var principal = manager.documentPrincipal;
        win.urlSecurityCheck(image.url, principal);
        var { E10SUtils } = ChromeUtils.importESModule("resource://gre/modules/E10SUtils.sys.mjs");
        var { PrivateBrowsingUtils } = ChromeUtils.importESModule("resource://gre/modules/PrivateBrowsingUtils.sys.mjs");
        win.internalSave(image.url, null, null, null, image.contentDisposition, image.contentType, false,
          "SaveImageTitle", null, E10SUtils.deserializeReferrerInfo(image.referrer),
          E10SUtils.deserializeCookieJarSettings(image.cookies), null, false, null,
          PrivateBrowsingUtils.isBrowserPrivate(browser), principal);
        return;
      }
    }

    // Failure of this optional feature must never stop the other enhancements.
    try {
      ChromeUtils.registerWindowActor("FirefoxEnhancementsHover", {
        child: { esModuleURI: "resource://artllex-shared/FirefoxEnhancementsHoverChild.sys.mjs" },
        allFrames: true,
        includeChrome: false,
        // Read-only child: no parent message handlers or privileged commands.
        safeForUntrustedWebProcess: true
      });
    } catch (error) { reportError(error); }

    function ensureShortcutDiagnostic(win) {
      if (win.__firefoxEnhancementsShortcutDiagnostic) return;
      win.__firefoxEnhancementsShortcutDiagnostic = true;
      var timer = win.setInterval(function () {
        try {
          if (Services.wm.getMostRecentWindow("navigator:browser") !== win) return;
          var signal = Services.dirsvc.get("TmpD", Ci.nsIFile).clone();
          signal.append("FirefoxEnhancementsCtrlShiftX.signal");
          if (!signal.exists()) return;
          signal.remove(false);
          var target = Services.wm.getMostRecentWindow("navigator:browser") || win;
          if (!saveContextImage(target)) saveHoveredImage(target).catch(reportError);
        } catch (error) { reportError(error); }
      }, 75);
      win.addEventListener("unload", function () { win.clearInterval(timer); }, { once: true });
    }
    function isSecretProfile() {
      try {
        return Services.dirsvc.get("ProfD", Ci.nsIFile).leafName === "FirefoxSecretProfile";
      } catch (_) {
        return false;
      }
    }

    function getSecretProfileMarker(name) {
      var file = Services.dirsvc.get("ProfD", Ci.nsIFile).clone();
      file.append(name);
      return file;
    }

    function touchFile(file) {
      try {
        if (!file.exists()) {
          file.create(Ci.nsIFile.NORMAL_FILE_TYPE, 384);
        }
      } catch (error) {
        reportError(error);
      }
    }

    function removeFileIfExists(file) {
      try {
        if (file.exists()) {
          file.remove(false);
        }
      } catch (_) {}
    }

    function setSecretProfileAudioMuted(shouldMute) {
      try {
        var windows = Services.wm.getEnumerator("navigator:browser");
        while (windows.hasMoreElements()) {
          var browserWin = windows.getNext();
          if (!browserWin || !browserWin.gBrowser) {
            continue;
          }
          var tabs = browserWin.gBrowser.tabs;
          for (var i = 0; i < tabs.length; i++) {
            var tab = tabs[i];
            if (shouldMute) {
              if (!secretMediaMuteState.has(tab)) {
                secretMediaMuteState.set(tab, !!tab.muted);
              }
              if (!tab.muted && typeof tab.toggleMuteAudio === "function") {
                tab.toggleMuteAudio();
              }
            } else if (secretMediaMuteState.has(tab)) {
              var wasMuted = secretMediaMuteState.get(tab);
              if (!wasMuted && tab.muted && typeof tab.toggleMuteAudio === "function") {
                tab.toggleMuteAudio();
              }
              secretMediaMuteState.delete(tab);
            }
          }
        }
      } catch (error) {
        reportError(error);
      }
    }

    function ensureSecretProfileMediaGuard(win) {
      try {
        if (!isSecretProfile() || secretMediaGuardStarted) {
          return;
        }
        secretMediaGuardStarted = true;

        var requestFile = getSecretProfileMarker(".secret-window-hidden");
        var ackFile = getSecretProfileMarker(".secret-window-hidden-ack");

        var timer = win.setInterval(function () {
          try {
            var requested = requestFile.exists();
            if (requested) {
              // Re-run while requested so any additional browser window/tab is also muted.
              setSecretProfileAudioMuted(true);
              secretMediaGuardHidden = true;
              touchFile(ackFile);
            } else if (secretMediaGuardHidden) {
              setSecretProfileAudioMuted(false);
              secretMediaGuardHidden = false;
              removeFileIfExists(ackFile);
            }
          } catch (error) {
            reportError(error);
          }
        }, 40);

        win.addEventListener(
          "unload",
          function () {
            try { win.clearInterval(timer); } catch (_) {}
          },
          { once: true }
        );
      } catch (error) {
        reportError(error);
      }
    }

    function getWScriptFile() {
      var file = Services.dirsvc.get("WinD", Ci.nsIFile).clone();
      file.append("System32");
      file.append("wscript.exe");
      return file;
    }

    function getSecretHelperFile() {
      var file = Services.dirsvc.get("GreD", Ci.nsIFile).clone();
      file.append(SECRET_HELPER_NAME);
      return file;
    }

    function getSecretLauncherFile() {
      var file = Services.dirsvc.get("GreD", Ci.nsIFile).clone();
      file.append(SECRET_LAUNCHER_NAME);
      return file;
    }

    function getFirefoxExeFile() {
      try {
        return Services.dirsvc.get("XREExeF", Ci.nsIFile).clone();
      } catch (_) {
        var file = Services.dirsvc.get("GreD", Ci.nsIFile).clone();
        file.append("firefox.exe");
        return file;
      }
    }

    function getFirefoxProcessId() {
      try {
        return Services.appinfo.processID;
      } catch (_) {
        try {
          return Services.appinfo.QueryInterface(Ci.nsIXULRuntime).processID;
        } catch (_) {
          return 0;
        }
      }
    }

    function launchSecretHelperMode(mode) {
      try {
        var helper = getSecretHelperFile();
        var launcher = getSecretLauncherFile();
        var firefoxExe = getFirefoxExeFile();
        var wscript = getWScriptFile();
        var pid = getFirefoxProcessId();

        if (!pid || !helper.exists() || !launcher.exists() || !firefoxExe.exists() || !wscript.exists()) {
          reportError("Secret window helper files are missing or PID is unavailable.");
          return false;
        }

        var process = Cc["@mozilla.org/process/util;1"].createInstance(Ci.nsIProcess);
        process.init(wscript);
        var args = [
          "//B",
          "//Nologo",
          launcher.path,
          helper.path,
          String(pid),
          firefoxExe.path,
          mode
        ];
        process.runwAsync(args, args.length, { observe: function () {} }, false);
        return true;
      } catch (error) {
        reportError(error);
        return false;
      }
    }

    function ensureSecretHelperStarted() {
      if (secretHelperStarted) {
        return;
      }
      if (launchSecretHelperMode("listen")) {
        secretHelperStarted = true;
      }
    }

    function getAppMenuNode(win, id) {
      try {
        var doc = win.document;
        var node = doc.getElementById(id);
        if (node) {
          return node;
        }
        var cache = doc.getElementById("appMenu-viewCache");
        if (cache && cache.content) {
          return cache.content.querySelector("#" + id);
        }
      } catch (error) {
        reportError(error);
      }
      return null;
    }

    function removeFollowingSeparator(node) {
      try {
        if (!node) {
          return;
        }
        var next = node.nextSibling;
        while (next && next.nodeType !== 1) {
          next = next.nextSibling;
        }
        if (next && next.localName === "toolbarseparator" && next.parentNode) {
          next.parentNode.removeChild(next);
        }
      } catch (error) {
        reportError(error);
      }
    }

    function moveHistoryActionsToMainMenu(win) {
      try {
        var historyButton = getAppMenuNode(win, MAIN_HISTORY_BUTTON_ID);
        var restoreButton = getAppMenuNode(win, RESTORE_SESSION_BUTTON_ID);
        var clearButton = getAppMenuNode(win, CLEAR_HISTORY_BUTTON_ID);

        if (!historyButton || !restoreButton || !clearButton || !historyButton.parentNode) {
          return false;
        }

        var mainBody = historyButton.parentNode;

        // If already moved, keep the native buttons exactly under History.
        if (
          restoreButton.parentNode === mainBody &&
          clearButton.parentNode === mainBody &&
          restoreButton.previousSibling === historyButton
        ) {
          return true;
        }

        // In the native History subview each of these buttons is followed by a
        // separator. Remove those separators before moving the native nodes,
        // otherwise the History subview would be left with two empty dividers.
        if (restoreButton.parentNode !== mainBody) {
          removeFollowingSeparator(restoreButton);
        }
        if (clearButton.parentNode !== mainBody) {
          removeFollowingSeparator(clearButton);
        }

        var anchor = historyButton.nextSibling;
        mainBody.insertBefore(restoreButton, anchor);
        mainBody.insertBefore(clearButton, anchor);

        restoreButton.setAttribute("firefox-quick-tools-main-menu", "true");
        clearButton.setAttribute("firefox-quick-tools-main-menu", "true");

        return true;
      } catch (error) {
        reportError(error);
        return false;
      }
    }

    function initMainMenuHistoryActions(win) {
      try {
        if (moveHistoryActionsToMainMenu(win)) {
          return;
        }

        var observer = new win.MutationObserver(function () {
          if (moveHistoryActionsToMainMenu(win)) {
            try {
              observer.disconnect();
            } catch (_) {}
          }
        });
        observer.observe(win.document.documentElement, {
          childList: true,
          subtree: true
        });

        win.addEventListener(
          "unload",
          function () {
            try {
              observer.disconnect();
            } catch (_) {}
          },
          { once: true }
        );
      } catch (error) {
        reportError(error);
      }
    }

    var IMAGE_CONTEXT_KEEP_IDS = new Set([
      "context-openlinkintab",
      "context-openlink",
      "context-copylink",
      "context-stripOnShareLink",
      "context-sendlinktodevice",
      "context-viewimage",
      "context-saveimage",
      "context-copyimage-contents",
      "context-copyimage",
      "context-setDesktopBackground",
      "context-inspect-a11y",
      "context-inspect"
    ]);

    var LINK_CONTEXT_KEEP_IDS = new Set([
      "context-openlinkintab",
      "context-openlink",
      "context-copylink",
      "context-stripOnShareLink",
      "context-sendlinktodevice",
      "context-inspect-a11y",
      "context-inspect"
    ]);

    function isExtensionContextItem(node) {
      try {
        if (
          node.id && (
            node.id.startsWith("webext-") ||
            node.id.startsWith("extension-") ||
            node.id.includes("-menuitem-")
          )
        ) return true;
        if (
          node.hasAttribute("data-extensionid") ||
          node.hasAttribute("generateditemid") ||
          node.hasAttribute("extension") ||
          Array.from(node.classList || []).some(function (name) {
            return name.includes("webextension") || name.includes("extension-menu");
          })
        ) return true;
        return !!node.querySelector(
          "[id^='webext-'], [id*='-menuitem-'], [data-extensionid], [generateditemid], [class*='webextension'], [class*='extension-menu']"
        );
      } catch (_) { return false; }
    }

    function moveSaveImageFirst(popup) {
      try {
        var item = popup.querySelector("#context-saveimage");
        if (!item || item.parentNode !== popup) return;
        var shortcutLabel = "(Ctrl+Shift+X)";
        var currentLabel = item.getAttribute("label") || item.label || "";
        if (currentLabel && !currentLabel.includes(shortcutLabel)) {
          item.removeAttribute("data-l10n-id");
          item.removeAttribute("data-l10n-args");
          item.setAttribute("label", currentLabel + "    " + shortcutLabel);
        }
        if (!item.hasAttribute("firefox-enhancements-original-position")) {
          item.setAttribute("firefox-enhancements-original-position", "true");
          item.__firefoxEnhancementsOriginalNextSibling = item.nextSibling;
        }
        popup.insertBefore(item, popup.firstChild);
      } catch (error) { reportError(error); }
    }

    function restoreSaveImagePosition(popup) {
      try {
        var item = popup.querySelector("#context-saveimage[firefox-enhancements-original-position]");
        if (!item) return;
        var next = item.__firefoxEnhancementsOriginalNextSibling;
        if (next && next.parentNode === popup) popup.insertBefore(item, next);
        else popup.appendChild(item);
        delete item.__firefoxEnhancementsOriginalNextSibling;
        item.removeAttribute("firefox-enhancements-original-position");
      } catch (error) { reportError(error); }
    }

    function moveExtensionItemsLast(popup) {
      try {
        if (popup.__firefoxEnhancementsExtensionPositions) return;
        var items = Array.from(popup.children).filter(isExtensionContextItem);
        popup.__firefoxEnhancementsExtensionPositions = items.map(function (item) {
          return { item: item, next: item.nextSibling };
        });
        for (var item of items) popup.appendChild(item);
      } catch (error) { reportError(error); }
    }

    function restoreExtensionItemPositions(popup) {
      try {
        var positions = popup.__firefoxEnhancementsExtensionPositions;
        if (!positions) return;
        for (var index = positions.length - 1; index >= 0; index--) {
          var position = positions[index];
          if (position.next && position.next.parentNode === popup) {
            popup.insertBefore(position.item, position.next);
          } else {
            popup.appendChild(position.item);
          }
        }
        delete popup.__firefoxEnhancementsExtensionPositions;
      } catch (error) { reportError(error); }
    }

    function restoreImageContextMenu(popup) {
      try {
        restoreExtensionItemPositions(popup);
        restoreSaveImagePosition(popup);
        for (var node of popup.querySelectorAll("[firefox-enhancements-context-hidden]")) {
          node.hidden = node.getAttribute("firefox-enhancements-context-hidden") === "true";
          node.removeAttribute("firefox-enhancements-context-hidden");
        }
      } catch (error) { reportError(error); }
    }

    function filterImageContextMenu(win, popup) {
      try {
        restoreImageContextMenu(popup);
        if (!win.gContextMenu) return;
        var keepIds = null;
        if (win.gContextMenu.onImage) keepIds = IMAGE_CONTEXT_KEEP_IDS;
        else if (win.gContextMenu.onLink) keepIds = LINK_CONTEXT_KEEP_IDS;
        if (!keepIds) return;
        for (var node of Array.from(popup.children)) {
          if (node.localName === "menuseparator") {
            node.setAttribute("firefox-enhancements-context-hidden", String(!!node.hidden));
            continue;
          }
          if (keepIds.has(node.id) || isExtensionContextItem(node)) continue;
          node.setAttribute("firefox-enhancements-context-hidden", String(!!node.hidden));
          node.hidden = true;
        }
        if (win.gContextMenu.onImage) moveSaveImageFirst(popup);
        moveExtensionItemsLast(popup);
        if (typeof win.gContextMenu.showHideSeparators === "function") {
          win.gContextMenu.showHideSeparators(popup);
        }
      } catch (error) { reportError(error); }
    }

    function initImageContextMenu(win) {
      try {
        var popup = win.document.getElementById("contentAreaContextMenu");
        if (!popup || popup.hasAttribute("firefox-enhancements-filter-ready")) return;
        popup.setAttribute("firefox-enhancements-filter-ready", "true");
        popup.addEventListener("popupshowing", function (event) {
          if (event.target === popup) filterImageContextMenu(win, popup);
        });
        popup.addEventListener("popuphidden", function (event) {
          if (event.target === popup) restoreImageContextMenu(popup);
        });
      } catch (error) { reportError(error); }
    }

    function reportError(error) { Components.utils.reportError(error); }
    try {
      ChromeUtils.importESModule('resource://artllex-shared/FirefoxEnhancementsLibrary.sys.mjs').start();
    } catch (error) { reportError(error); }
    function initWindow(win) {
      ensureShortcutDiagnostic(win);
      ensureSecretProfileMediaGuard(win);
      if (!isSecretProfile()) ensureSecretHelperStarted();
      initMainMenuHistoryActions(win);
      initImageContextMenu(win);
    }
    Services.obs.addObserver({observe: function(win) {win.setTimeout(function(){initWindow(win);},0);}}, "browser-delayed-startup-finished");
  })();
} catch(error) { Components.utils.reportError(error); }
  // Only load the other product while its own preference file is active.
  try {
    var peer = Services.dirsvc.get("GreD", Ci.nsIFile);
    peer.append("download-router-support.cfg");
    var enabled = Services.dirsvc.get("GreD", Ci.nsIFile);
    enabled.append("defaults"); enabled.append("pref"); enabled.append("download-router-support.js");
    if (enabled.exists() && peer.exists()) {
      var rootURI = Services.io.newFileURI(Services.dirsvc.get("GreD", Ci.nsIFile));
      Services.io.getProtocolHandler("resource").QueryInterface(Ci.nsIResProtocolHandler)
        .setSubstitution("artllex-cooperative", rootURI);
      Services.scriptloader.loadSubScript("resource://artllex-cooperative/" + peer.leafName, globalThis, "UTF-8");
    }
  } catch (error) { Components.utils.reportError(error); }
})();
